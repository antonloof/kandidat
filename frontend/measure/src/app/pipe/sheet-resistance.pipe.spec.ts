import { SheetResistancePipe } from './sheet-resistance.pipe';

describe('SheetResistancePipe', () => {
  it('create an instance', () => {
    const pipe = new SheetResistancePipe();
    expect(pipe).toBeTruthy();
  });
});
